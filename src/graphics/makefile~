#=================================================================================================
#
#
#
#=================================================================================================
#Source Code Files
SRCS = graphics.cpp
HDRS = $(SRCS:.cpp=.h)
OBJS = $(SRCS:.cpp=.o)
OUT_PATH = ../../objs
#=================================================================================================
include ../../makevar
#SDL and -lSDL_image
#CFLAGS += $(shell pkg-config --cflags sdl)
#LIBS += $(shell pkg-config --libs sdl) -lSDL_image
#=================================================================================================
#Open GL and GLU
#LIBS += -lGL -lGLU
#=================================================================================================
#sigc++
#CFLAGS += $(shell pkg-config --cflags sigc++-2.0)
#LIBS += $(shell pkg-config --libs sigc++-2.0)
#=================================================================================================
CFLAGS = $(SDL_CFLAGS)
LIBS   = $(SDL_LIBS) $(GL_LIBS)
#=================================================================================================
all: $(OBJS)
graphics.o: $(SRCS) $(HDRS)
	g++ -c -fpic $(LIBS) $(CFLAGS) $(SRCS) -o $(OUT_PATH)/graphics.o
#=================================================================================================
clean:
	-rm ../../$(OBJS)
#=================================================================================================

