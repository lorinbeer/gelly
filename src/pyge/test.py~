from graphics import Graphics
from dungeon.dungeon import Tile
#from actor import TileActor_gl
from actor import Actor
import pygame

clock = pygame.time.Clock()

pygame.init()

gfx  = Graphics()
gfx1 = Graphics()
gfx2 = Graphics()
gfx3 = Graphics()
tile = Actor( [ (0,0), (0,181), (100,181), (100,0) ],
              [ (0,0), (0,181), (100,181), (100,0) ],
              "/home/lorin/projects/ge/art/planetcute/Stone Block.png" )

tile.draw( (100,100) )

while True:
  timepassed = clock.tick( 10 ) #FPS
  gfx.display()
  pygame.display.flip()
